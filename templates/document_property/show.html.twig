{% extends 'base.html.twig' %}

{% block title %}DocumentProperty{% endblock %}

{% block body %}
    <h1>DocumentProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_property.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ document_property.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ document_property.type }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ document_property.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('document_property_index') }}">back to list</a>

    <a href="{{ path('document_property_edit', {'id': document_property.id}) }}">edit</a>

    {{ include('document_property/_delete_form.html.twig') }}
{% endblock %}
