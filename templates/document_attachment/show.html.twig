{% extends 'base.html.twig' %}

{% block title %}DocumentAttachment{% endblock %}

{% block body %}
    <h1>DocumentAttachment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_attachment.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ document_attachment.type }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ document_attachment.path }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('document_attachment_index') }}">back to list</a>

    <a href="{{ path('document_attachment_edit', {'id': document_attachment.id}) }}">edit</a>

    {{ include('document_attachment/_delete_form.html.twig') }}
{% endblock %}
